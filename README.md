# Strangely Odd — cover art

Public host for per-episode cover images (3000×3000) so the podcast host can fetch them by URL.
Images only — the show's production repo (scripts, puzzle answers) is private.
